name: "Pull Request Labeler"

on:
  pull_request:
    branches:
      - "main"

permissions:
  contents: read
  pull-requests: write

jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5
